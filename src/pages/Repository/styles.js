import styled from 'styled-components/native';
import { WebView } from 'react-native-webview';

export const Container = styled.View`
  flex: 1;
  margin: 5px;
`;

export const ViewRepository = styled(WebView)`
  flex: 1;
  margin: 5px;
`;
